# Copy to .env and run: source .env && go run ./cmd/goflow
# Start Postgres first: docker compose up -d
# Compose uses port 5433 to avoid conflict with local Postgres on 5432.
DATABASE_URL=postgres://goflow:goflow@localhost:5433/goflow?sslmode=disable
# WORKER_COUNT=3
